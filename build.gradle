task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}

buildscript {
    repositories {
        jcenter()
        mavenLocal()
    }
    dependencies {
        classpath "org.springframework.boot:spring-boot-gradle-plugin:1.2.4.RELEASE"
        classpath "com.kiefer.gradle:ember-cli-gradle-plugin:2.0.0"
    }
}

allprojects {
    repositories {
        jcenter()
        mavenCentral()
    }
}

group = "com.kiefer.gradle"
version = "1.0.0"



